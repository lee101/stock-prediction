[build-system]
requires = ["setuptools>=64", "wheel", "torch>=2.5"]
build-backend = "setuptools.build_meta"

[project]
name = "gpu_trading_env"
version = "0.1.0"
description = "SoA fused env_step CUDA kernel for GPU-resident RL trading"
requires-python = ">=3.10"
